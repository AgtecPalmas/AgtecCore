name: $AppPackage$
description: $AppDescription$

version: 1.0.0+1

environment:
  sdk: ^3.7.2


dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  dio: ^5.8.0+1 #Lib para acesso HTTP para consumir APIRest.
  path: ^1.9.1 #Lib para auxiliar na manipulação de arquivos.
  intl: ^0.20.2 #Lib para formatação de dados como Date e DateTime.
  date_format: ^2.0.9 #Lib para formatação trabalhando apenas com Data.
  url_launcher: ^6.3.1 #Lib para invocar intents dos devices como por exemplo envio de email.
  transparent_image: ^2.0.1 #Lib que disponibiliza uma imagem transparente, usada para auxiliar o download de imagens da web funcionando como placeholder.
  cached_network_image: ^3.4.1 #Lib para realizar cache das imagens baixadas da WEB.
  connectivity_plus: ^6.1.4 #Lib para auxiliar a checagem de conectividade do device.
  google_sign_in: ^6.3.0 #Lib para autenticação com contas Google.
  image_picker: ^1.1.2 #Lib para realizar crop e rotação em imagens.
  shared_preferences: ^2.5.3 #Lib para acessar o SharedPreferences do device.
  location: ^8.0.0 #Lib para localização do usuário e utilização de mapa se necessário.
  flutter_keyboard_visibility: ^6.0.0
  path_provider: ^2.1.5
  webview_flutter: ^4.11.0
  flutter_launcher_icons: ^0.14.3 #Lib para gerar o ícone do aplicativo para quando for submetido para as lojas.
  font_awesome_flutter: ^10.8.0 #Lib para utilização dos ícones do site Font Awesome.
  sembast: ^3.8.5 #Banco de dados NoSQL
  flutter_screenutil: ^5.9.3 #Lib para auxiliar no cálculo de densidade da tela
  top_snackbar_flutter: ^3.2.0 #Lib para exibir mensagens de sucesso ou erro na tela.
  loading_animation_widget: ^1.3.0 #Lib para exibir animações de loading na tela.

  # Libs do Cubit
  equatable: ^2.0.7
  bloc: ^9.0.0
  flutter_bloc: ^9.1.0

  # Firebase
  firebase_messaging: ^15.2.5 #Lib para recebimento de mensagens push.
  firebase_auth: ^5.5.2 #Lib para autenticação com contas Firebase.
  firebase_core: ^3.13.0 #lib para trabalhar com o Firebase
  firebase_crashlytics: ^4.3.5
  firebase_remote_config: ^5.4.3 # Lib para trabalhar com configurações remotas do Firebase.

  # Sentry Plataforma de monitoramento de bugs
  sentry_flutter: ^8.14.2

  # Pacote para auxiliar a geração de logs no terminal
  logger: ^2.5.0

  # Pacote para auxiliar na validação de campos
  validatorless: ^1.2.4

dev_dependencies:
  flutter_test:
    sdk: flutter

  # The "flutter_lints" package below contains a set of recommended lints to
  # encourage good coding practices. The lint set provided by the package is
  # activated in the `analysis_options.yaml` file located at the root of your
  # package. See that file for information about deactivating specific lint
  # rules and activating additional ones.
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
  assets:
    # Arquivos de idioma em formato Json utilizados na internacionalização da APP
    - lang/en.json
    - lang/pt.json