/// Classe para controlar as chaves que serão utilizadas no
/// secure storage para os valores da camada de HTTP
///


class HTTPStorageKeys {
  static const String token = 'token';
  static const String refreshToken = 'refreshToken';
}
