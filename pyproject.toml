[project]
name = "agteccore"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = "==3.12.*"
dependencies = [
    "django==5.2.12",
    "jinja2>=3.1.6",
]

[tool.taskipy.settings]
user_vars = true

[tool.pytest.ini_options]
addopts = "--cov-report=html --doctest-modules --doctest-glob='*.rst' --ignore=core --ignore=docs --ignore=venv --ignore=.venv --ignore=.env"

[tool.ruff]
line-length = 120
target-version = "py312"
extend-exclude = [
  "core",
  "docs",
  ".venv",
  "migrations",
  ".env"
]

[tool.ruff.lint]
select = ["E", "F", "I"]
ignore = ["E203", "W503"]

[tool.ruff.format]
quote-style = "double"

[dependency-groups]
dev = [
    "pytest>=9.0.3",
    "pytest-cov>=7.1.0",
]

[tool.taskipy.tasks]
build_all = "python manage.py build --all"
build_force = "python manage.py build --all --force"
core_checkupdate = "python manage.py core --checkupdate"
core_version = "python manage.py core --version"
coverage = "coverage html"
docs = "mkdocs serve"
lint = "ruff check ."
fix = "ruff check . --fix"
format = "ruff format ."
mgt = "python manage.py migrate"
mkm = "python manage.py makemigrations"
post_test = "coverage html"
pre_test = "task lint"
run = "python manage.py runserver"
runserver = "python manage.py runserver"
shell = "python manage.py shell"
startapp = "python manage.py startapp"
test = "pytest -s -x --cov=projeto_base -vv"
